mod my_priority_queue;

mod no_priority_queue;
mod ordinary_priority_queue;

#[cfg(feature = "dependency")]
mod dependency_queue;

mod israeli_priority;

mod nested_queue;

fn main() {
    println!("Hello, World!");
}
